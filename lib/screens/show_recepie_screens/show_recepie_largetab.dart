import 'package:flutter/material.dart';

class ShowRecepieLargetab extends StatelessWidget {
  const ShowRecepieLargetab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
